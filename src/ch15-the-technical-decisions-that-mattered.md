# The technical decisions that mattered — stateless HTTP, URLs without coordination, lossy linking, view-source

*Placeholder — the specific design choices that gave the web its shape: stateless HTTP, uncoordinated URLs, lossy one-way links, and view-source as a teaching tool.*
